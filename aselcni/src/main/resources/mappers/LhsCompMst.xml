<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aselcni.lhs.LhsCustMst">
	
	<update id="compmstUpdate" parameterType="LhsCompMst">
		merge into TB_COMPMST as c1
		using (select count(*) cnt from TB_COMPMST) as c2
		on (c2.cnt = 1)
		when matched then
		 update
		 set 
		 	comp_busi_no = #{comp_busi_no},
		 	comp_nm = #{comp_nm},
		 	comp_president_nm = #{comp_president_nm},
		 	comp_cond = #{comp_cond},
		 	comp_item = #{comp_item},
		 	comp_addr = #{comp_addr},
		 	comp_tel = #{comp_tel},
		 	comp_fax = #{comp_fax},
		 	<if test="comp_image != null">
		 		comp_image = CONVERT(image,#{comp_image}),
		 	</if>
		 	comp_update = CONVERT(CHAR(10),GETDATE(),23)
		 
		when not matched then
		 insert
		 values(
		 	#{comp_busi_no},
		 	#{comp_nm},
		 	#{comp_president_nm},
		 	#{comp_cond},
		 	#{comp_item},
		 	#{comp_addr},
		 	#{comp_tel},
		 	#{comp_fax},
		 	<if test="comp_image != null">
		 		CONVERT(image,#{comp_image}),
		 	</if>
		 	<if test="comp_image == null">
		 		null,
		 	</if>
		 	null
		 );
	</update>
    
    <select id="compmstSelect" resultType="LhsCompMst">
    	select top 1 *
    	from tb_compmst
    </select>
</mapper>