<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aselcni.KphTypeMidMapper">
	
	<select id="KphGetTypeMidList" parameterType="KphTypeMid" resultType="KphTypeMid">
		SELECT *
		FROM TB_TYPE_MID
		WHERE BIG_NO = #{big_no}
			AND MID_NO IN (
									SELECT MID_NO
									FROM TB_ITEMMST
									WHERE BIG_NO = #{big_no} 	
									GROUP BY MID_NO
									)
	</select>				
	
</mapper>