<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aselcni.UjmOutitemMapper">

	<!-- 소문자 사용 -->
	
	
	
	
	<select id="ujmTotalOutitemCnt" resultType="int">
		SELECT Count(*) from tb_outitem where outitem_delete_chk=0
	</select>
	
	<select id="ujmListOutitemAll" parameterType="UjmOutitem" resultType="UjmOutitem">
		<![CDATA[
			SELECT a.outitem_no, a.seq_no, a.ord FROM (
    			SELECT ROW_NUMBER() OVER (ORDER BY outitem_no) AS rn, a.*
    			FROM TB_OUTITEM a
    			WHERE a.outitem_delete_chk = 0
				) AS sub
				WHERE rn >= #{start} AND rn <= #{end}
		]]> 
	</select>
	
	
	
</mapper>