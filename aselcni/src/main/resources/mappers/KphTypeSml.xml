<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aselcni.KphTypeSmlMapper">
	
	<select id="KphGetTypeSmlList" parameterType="KphTypeMid" resultType="KphTypeSml">
		SELECT *
		FROM TB_TYPE_SML
		WHERE BIG_NO = #{big_no}
			AND MID_NO = #{mid_no}
			AND SML_NO IN (
									SELECT SML_NO
									FROM TB_ITEMMST
									WHERE BIG_NO = #{big_no}
									AND MID_NO = #{mid_no} 	
									GROUP BY SML_NO
									)
	</select>				
	
</mapper>